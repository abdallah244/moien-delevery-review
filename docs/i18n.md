# 🌍 دليل الترجمة | Internationalization Guide

<div align="center">

[![Languages](https://img.shields.io/badge/Languages-7-blue.svg)]()

**دليل دعم اللغات المتعددة في Moien Delivery**

</div>

---

## 📋 اللغات المدعومة

| اللغة             | الكود | الحالة            |
| ----------------- | ----- | ----------------- |
| 🇬🇧 English        | `en`  | ✅ مدعومة         |
| 🇫🇷 Français       | `fr`  | ✅ مدعومة         |
| 🇱🇺 Lëtzebuergesch | `lb`  | ✅ مدعومة         |
| 🇩🇪 Deutsch        | `de`  | ✅ مدعومة         |
| 🇮🇹 Italiano       | `it`  | ✅ مدعومة         |
| 🇵🇹 Português      | `pt`  | ✅ مدعومة         |
| 🇪🇸 Español        | `es`  | ✅ مدعومة         |
| 🇸🇦 العربية        | `ar`  | ✅ مدعومة (Admin) |

---

## 🌐 Angular (Web Frontend) - مكتمل ✅

### Language Service

```typescript
// services/language.ts
import { Injectable, signal, computed, effect } from "@angular/core";

export interface Language {
  code: string;
  name: string;
  flag: string;
}

@Injectable({ providedIn: "root" })
export class LanguageService {
  readonly languages: Language[] = [
    { code: "en", name: "English", flag: "🇬🇧" },
    { code: "fr", name: "Français", flag: "🇫🇷" },
    { code: "lb", name: "Lëtzebuergesch", flag: "🇱🇺" },
    { code: "de", name: "Deutsch", flag: "🇩🇪" },
    { code: "it", name: "Italiano", flag: "🇮🇹" },
    { code: "pt", name: "Português", flag: "🇵🇹" },
    { code: "es", name: "Español", flag: "🇪🇸" },
  ];

  private currentLangCode = signal<string>("en");
  currentLanguage = computed(() => this.languages.find((l) => l.code === this.currentLangCode())!);

  // الترجمات لجميع اللغات
  private translations: Record<string, Record<string, any>> = {
    en: {
      navbar: {
        location: "Enter delivery address",
        search: "Search restaurants or cuisines...",
        signIn: "Sign in",
        signUp: "Sign up",
      },
      footer: {
        partner: "Partner with us",
        restaurant: "For restaurants",
        deliver: "Become a courier",
        merchant: "Moien for merchants",
        company: "Company",
        about: "About us",
        jobs: "Jobs",
        contact: "Contact",
        // ... المزيد
      },
    },
    // نفس الهيكل للغات الأخرى
  };

  t(key: string): string {
    const keys = key.split(".");
    let value: any = this.translations[this.currentLangCode()];
    for (const k of keys) {
      value = value?.[k];
    }
    return value || key;
  }

  setLanguage(code: string) {
    this.currentLangCode.set(code);
    localStorage.setItem("moien-lang", code);
  }
}
```

### Admin (لوحة الأدمن) - EN/AR

واجهة الأدمن لديها خدمة ترجمة منفصلة لدعم الإنجليزية والعربية:

- Service: `services/admin-language.ts`
- Languages: `en`, `ar`

> ملاحظة: ترجمة المستخدم (User) وترجمة الأدمن (Admin) منفصلتين حتى لا تختلط مفاتيح النصوص.

### استخدام الخدمة في المكونات

```typescript
import { Component, inject } from "@angular/core";
import { LanguageService } from "./services/language";

@Component({
  selector: "app-navbar",
  template: `
    <input [placeholder]="lang.t('navbar.search')" />
    <button>{{ lang.t("navbar.signIn") }}</button>
  `,
})
export class NavbarComponent {
  lang = inject(LanguageService);
}
```

### Language Modal

```typescript
import { Component, inject, output } from "@angular/core";
import { LanguageService } from "../../services/language";

@Component({
  selector: "app-language-modal",
  template: `
    <div class="modal-overlay" (click)="close.emit()">
      <div class="modal-content">
        <h2>Select Language</h2>
        @for (language of lang.languages; track language.code) {
          <button [class.active]="language.code === lang.currentLanguage().code" (click)="selectLanguage(language.code)">{{ language.flag }} {{ language.name }}</button>
        }
      </div>
    </div>
  `,
})
export class LanguageModalComponent {
  lang = inject(LanguageService);
  close = output<void>();

  selectLanguage(code: string) {
    this.lang.setLanguage(code);
    this.close.emit();
  }
}
```

---

## 🔧 الإعداد

### Flutter (Mobile)

```yaml
# pubspec.yaml
dependencies:
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0
```

```dart
// lib/main.dart
MaterialApp(
  localizationsDelegates: [
    AppLocalizations.delegate,
    GlobalMaterialLocalizations.delegate,
    GlobalWidgetsLocalizations.delegate,
    GlobalCupertinoLocalizations.delegate,
  ],
  supportedLocales: [
    Locale('ar'),
    Locale('lb'),
  ],
  locale: Locale('ar'),
)
```

### Angular (Web) - الطريقة القديمة (ngx-translate)

```typescript
// app.config.ts
import { provideTranslateService } from "@ngx-translate/core";

export const appConfig: ApplicationConfig = {
  providers: [
    provideTranslateService({
      defaultLanguage: "ar",
      loader: {
        provide: TranslateLoader,
        useFactory: HttpLoaderFactory,
        deps: [HttpClient],
      },
    }),
  ],
};
```

---

## 📁 ملفات الترجمة

### Flutter (ARB Files)

```
lib/l10n/
├── app_ar.arb        # العربية
├── app_lb.arb        # Lëtzebuergesch
└── app_en.arb        # English
```

#### مثال: app_ar.arb

```json
{
  "@@locale": "ar",
  "appName": "موين دليفري",
  "home": "الرئيسية",
  "orders": "الطلبات",
  "profile": "الملف الشخصي",
  "search": "ابحث عن مطاعم أو أطباق...",
  "nearbyRestaurants": "مطاعم قريبة منك",
  "viewAll": "عرض الكل",
  "addToCart": "أضف للسلة",
  "checkout": "إتمام الطلب",
  "trackOrder": "تتبع الطلب",
  "orderPlaced": "تم استلام طلبك",
  "preparing": "جاري التحضير",
  "onTheWay": "في الطريق إليك",
  "delivered": "تم التوصيل",
  "deliveryAddress": "عنوان التوصيل",
  "paymentMethod": "طريقة الدفع",
  "cash": "نقداً عند الاستلام",
  "card": "بطاقة ائتمان",
  "total": "الإجمالي",
  "confirmOrder": "تأكيد الطلب",
  "@appName": {
    "description": "اسم التطبيق"
  }
}
```

#### مثال: app_lb.arb

```json
{
  "@@locale": "lb",
  "appName": "Moien Delivery",
  "home": "Doheem",
  "orders": "Bestellungen",
  "profile": "Profil",
  "search": "Sich no Restauranten oder Wieder...",
  "nearbyRestaurants": "Restauranten an Ärer Géigend",
  "viewAll": "All weisen",
  "addToCart": "An de Kuerf",
  "checkout": "Bestellen",
  "trackOrder": "Bestellung verfollegen",
  "orderPlaced": "Bestellung ugeholl",
  "preparing": "Gëtt zoubereet",
  "onTheWay": "Ënnerwee",
  "delivered": "Geliwwert",
  "deliveryAddress": "Liwweradress",
  "paymentMethod": "Bezuelmethod",
  "cash": "Bar bei Liwwerung",
  "card": "Kreditkaart",
  "total": "Gesammt",
  "confirmOrder": "Bestellung bestätegen"
}
```

### Angular (JSON Files)

```
src/assets/i18n/
├── ar.json           # العربية
├── lb.json           # Lëtzebuergesch
└── en.json           # English
```

#### مثال: ar.json

```json
{
  "COMMON": {
    "SAVE": "حفظ",
    "CANCEL": "إلغاء",
    "DELETE": "حذف",
    "EDIT": "تعديل",
    "CONFIRM": "تأكيد",
    "SEARCH": "بحث",
    "LOADING": "جاري التحميل...",
    "ERROR": "حدث خطأ",
    "SUCCESS": "تمت العملية بنجاح"
  },
  "LANDING": {
    "HERO": {
      "TITLE": "طعامك المفضل، بين يديك",
      "SUBTITLE": "اطلب من أفضل المطاعم واستلم طلبك في دقائق",
      "CTA": "ابدأ الطلب الآن"
    },
    "FEATURES": {
      "TITLE": "لماذا موين دليفري؟",
      "FAST": {
        "TITLE": "توصيل سريع",
        "DESC": "استلم طلبك في أقل من 30 دقيقة"
      },
      "QUALITY": {
        "TITLE": "جودة عالية",
        "DESC": "نختار لك أفضل المطاعم"
      },
      "TRACKING": {
        "TITLE": "تتبع مباشر",
        "DESC": "تابع طلبك لحظة بلحظة"
      }
    }
  },
  "ADMIN": {
    "DASHBOARD": {
      "TITLE": "لوحة التحكم",
      "ORDERS_TODAY": "طلبات اليوم",
      "REVENUE": "الإيرادات",
      "NEW_USERS": "مستخدمون جدد",
      "ACTIVE_DRIVERS": "سائقون نشطون"
    },
    "SIDEBAR": {
      "DASHBOARD": "لوحة التحكم",
      "USERS": "المستخدمين",
      "RESTAURANTS": "المطاعم",
      "ORDERS": "الطلبات",
      "DRIVERS": "السائقين",
      "PROMOTIONS": "العروض",
      "REPORTS": "التقارير",
      "SETTINGS": "الإعدادات"
    }
  }
}
```

#### مثال: lb.json

```json
{
  "COMMON": {
    "SAVE": "Späicheren",
    "CANCEL": "Ofbriechen",
    "DELETE": "Läschen",
    "EDIT": "Änneren",
    "CONFIRM": "Bestätegen",
    "SEARCH": "Sichen",
    "LOADING": "Gëtt gelueden...",
    "ERROR": "Feeler",
    "SUCCESS": "Erfollegräich"
  },
  "LANDING": {
    "HERO": {
      "TITLE": "Äert Liiblingsiessen, bei Iech doheem",
      "SUBTITLE": "Bestellt vun de beschte Restauranten a kritt Är Bestellung a Minutten",
      "CTA": "Elo bestellen"
    },
    "FEATURES": {
      "TITLE": "Firwat Moien Delivery?",
      "FAST": {
        "TITLE": "Schnell Liwwerung",
        "DESC": "Kritt Är Bestellung an manner wéi 30 Minutten"
      },
      "QUALITY": {
        "TITLE": "Héich Qualitéit",
        "DESC": "Mir wielen déi bescht Restaurante fir Iech"
      },
      "TRACKING": {
        "TITLE": "Live Tracking",
        "DESC": "Verfollegt Är Bestellung live"
      }
    }
  },
  "ADMIN": {
    "DASHBOARD": {
      "TITLE": "Dashboard",
      "ORDERS_TODAY": "Bestellungen haut",
      "REVENUE": "Recetten",
      "NEW_USERS": "Nei Benotzer",
      "ACTIVE_DRIVERS": "Aktiv Liwwerer"
    }
  }
}
```

---

## 💻 الاستخدام في الكود

### Flutter

```dart
// الاستخدام في Widget
Text(AppLocalizations.of(context)!.appName)

// أو باستخدام extension
Text(context.l10n.appName)

// تغيير اللغة
context.read<LocaleBloc>().add(ChangeLocale(Locale('lb')));
```

### Angular

```typescript
// في Component
constructor(private translate: TranslateService) {
  this.translate.use('ar');
}

// في Template
{{ 'LANDING.HERO.TITLE' | translate }}

// تغيير اللغة
this.translate.use('lb');
```

---

## 🔄 تبديل اللغة

### Flutter Widget

```dart
class LanguageSwitcher extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return PopupMenuButton<Locale>(
      icon: Icon(Icons.language),
      onSelected: (Locale locale) {
        context.read<LocaleBloc>().add(ChangeLocale(locale));
      },
      itemBuilder: (context) => [
        PopupMenuItem(
          value: Locale('ar'),
          child: Row(
            children: [
              Text('🇸🇦'),
              SizedBox(width: 8),
              Text('العربية'),
            ],
          ),
        ),
        PopupMenuItem(
          value: Locale('lb'),
          child: Row(
            children: [
              Text('🇱🇺'),
              SizedBox(width: 8),
              Text('Lëtzebuergesch'),
            ],
          ),
        ),
      ],
    );
  }
}
```

### Angular Component

```typescript
@Component({
  selector: "app-language-switcher",
  template: `
    <button mat-icon-button [matMenuTriggerFor]="langMenu">
      <mat-icon>language</mat-icon>
    </button>
    <mat-menu #langMenu="matMenu">
      <button mat-menu-item (click)="changeLanguage('ar')">🇸🇦 العربية</button>
      <button mat-menu-item (click)="changeLanguage('lb')">🇱🇺 Lëtzebuergesch</button>
    </mat-menu>
  `,
})
export class LanguageSwitcherComponent {
  constructor(private translate: TranslateService) {}

  changeLanguage(lang: string) {
    this.translate.use(lang);
    document.documentElement.dir = lang === "ar" ? "rtl" : "ltr";
    document.documentElement.lang = lang;
  }
}
```

---

## 📐 دعم RTL

### Flutter

```dart
MaterialApp(
  builder: (context, child) {
    return Directionality(
      textDirection: Localizations.localeOf(context).languageCode == 'ar'
          ? TextDirection.rtl
          : TextDirection.ltr,
      child: child!,
    );
  },
)
```

### Angular/CSS

```scss
// styles.scss
[dir="rtl"] {
  .sidebar {
    right: 0;
    left: auto;
  }

  .text-start {
    text-align: right;
  }

  .me-2 {
    margin-left: 0.5rem;
    margin-right: 0;
  }
}
```

---

## 🤝 المساهمة في الترجمة

1. انسخ ملف الترجمة الموجود
2. ترجم النصوص للغة المطلوبة
3. أضف اللغة للإعدادات
4. اختبر التطبيق
5. أرسل Pull Request

---

<div align="center">

[🔙 العودة للتوثيق](README.md)

</div>
