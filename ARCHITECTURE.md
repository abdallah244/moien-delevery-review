# 🏗️ Moien Delivery - Architecture Document | وثيقة الهيكل المعماري

<div align="center">

[![Architecture](https://img.shields.io/badge/Architecture-Monorepo%20%2B%20Modular-blue.svg)]()
[![Pattern](https://img.shields.io/badge/Pattern-Clean%20Architecture-green.svg)]()
[![API](https://img.shields.io/badge/API-RESTful-orange.svg)]()

</div>

---

## 📋 جدول المحتويات

- [نظرة عامة](#-نظرة-عامة)
- [المخطط المعماري العام](#-المخطط-المعماري-العام)
- [البنية الخلفية (Backend)](#-البنية-الخلفية-backend)
- [واجهة الويب (Web Frontend)](#-واجهة-الويب-web-frontend)
- [تطبيقات الموبايل (Mobile Apps)](#-تطبيقات-الموبايل-mobile-apps)
- [قاعدة البيانات](#-قاعدة-البيانات)
- [الأمان والمصادقة](#-الأمان-والمصادقة)
- [التكامل مع الخدمات الخارجية](#-التكامل-مع-الخدمات-الخارجية)
- [قابلية التوسع](#-قابلية-التوسع)

---

## 🎯 نظرة عامة

### الفلسفة المعمارية

يتبع **Moien Delivery** مبادئ **Clean Architecture** و **Domain-Driven Design (DDD)** لضمان:

- ✅ **قابلية الصيانة** - كود نظيف ومنظم
- ✅ **قابلية التوسع** - سهولة إضافة ميزات جديدة
- ✅ **قابلية الاختبار** - تغطية اختبارات شاملة
- ✅ **الاستقلالية** - فصل الاهتمامات بين الطبقات

### المكونات الرئيسية (الواقع الحالي)

```
┌─────────────────────────────────────────────────────────────────┐
│                        Client Layer                              │
├─────────────────┬─────────────────┬─────────────────────────────┤
│   Web App       │   Customer App  │   Driver App                │
│   (Angular)     │   (Flutter)     │   (Flutter)                 │
└────────┬────────┴────────┬────────┴────────┬────────────────────┘
         │                 │                  │
         ▼                 ▼                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Backend (NestJS)                            │
│                  Modular Monolith (Modules)                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                API Layer (/api/v1) + WebSockets                  │
├─────────┬─────────┬─────────┬────────────┬─────────┬────────────┤
│  Auth   │  Users  │ Orders  │ Restaurants│ Payments│ SiteSettings│
└─────────┴─────────┴─────────┴────────────┴─────────┴────────────┘
     │         │         │         │          │          │
     ▼         ▼         ▼         ▼          ▼          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Data Layer                                  │
├────────────────┬────────────────┬───────────────────────────────┤
│   PostgreSQL   │     Redis      │         S3/Storage            │
│   (Primary DB) │    (Cache)     │        (Files)                │
└────────────────┴────────────────┴───────────────────────────────┘
```

---

## 🔧 البنية الخلفية (Backend)

### التقنية: NestJS 11 + TypeScript

### هيكل المجلدات

```
backend/
├── src/
│   ├── main.ts                    # نقطة الدخول
│   ├── app.module.ts              # الوحدة الرئيسية
│   │
│   ├── modules/                   # ✅ الوحدات (Modules)
│   │   ├── admin-staff/           # 👥 إدارة الموظفين
│   │   │   ├── admin-staff.controller.ts
│   │   │   ├── admin-staff.service.ts
│   │   │   ├── admin-staff.module.ts
│   │   │   ├── admin-staff.entity.ts
│   │   │   └── dto/
│   │   │
│   │   ├── users/                 # 👤 المستخدمين
│   │   │   ├── users.controller.ts
│   │   │   ├── users.service.ts
│   │   │   ├── users-billing.service.ts   # Stripe
│   │   │   ├── users-addresses.service.ts # العناوين
│   │   │   ├── users.module.ts
│   │   │   ├── users.entity.ts
│   │   │   ├── user-address.entity.ts
│   │   │   ├── user-payment-method.entity.ts
│   │   │   ├── users.gateway.ts    # WebSocket
│   │   │   └── dto/
│   │   │
│   │   ├── support-tickets/       # 🎫 تذاكر الدعم
│   │   │   ├── support-tickets.controller.ts
│   │   │   ├── support-tickets.service.ts
│   │   │   ├── support-tickets.module.ts
│   │   │   └── dto/
│   │   │
│   │   └── payments/              # 💳 المدفوعات
│   │       ├── payments.controller.ts
│   │       └── payments.module.ts
│   │
│   ├── common/                    # المشترك
│   │   ├── decorators/            # المزخرفات
│   │   ├── filters/               # مرشحات الاستثناءات
│   │   ├── interceptors/          # المعترضات
│   │   ├── pipes/                 # الأنابيب
│   │   ├── utils/                 # الأدوات
│   │   └── services/              # ✅ 22 خدمة متكاملة
│   │       ├── security/          # 🔐 Rate Limiter, Encryption, JWT, IP Guard, Sanitizer
│   │       ├── performance/       # ⚡ Cache, Compression, Query Optimizer, Connection Pool, Response Optimizer
│   │       ├── realtime/          # 🔄 WebSocket Service
│   │       ├── ui/                # 🎨 Loading, Toast, Validation
│   │       ├── communication/     # 📧 Email, SMS, Notification
│   │       ├── storage/           # 📁 File Upload
│   │       ├── monitoring/        # 📊 Logger
│   │       ├── location/          # 📍 Geolocation
│   │       ├── search/            # 🔍 Full-text Search
│   │       ├── analytics/         # 📈 Analytics & Metrics
│   │       ├── queue/             # 📬 Background Jobs
│   │       └── backup/            # 💾 Database Backup & Restore
│   │
│   ├── config/                    # الإعدادات
│   │   ├── database.config.ts
│   │   ├── jwt.config.ts
│   │   └── app.config.ts
│   │
│   └── database/                  # قاعدة البيانات
│       ├── migrations/
│       └── seeders/
│
├── test/                          # الاختبارات
│   ├── unit/
│   ├── integration/
│   └── e2e/
│
└── docker/                        # Docker
    ├── Dockerfile
    └── docker-compose.yml
```

### الأنماط المستخدمة

| النمط                   | الاستخدام                 |
| ----------------------- | ------------------------- |
| **Repository Pattern**  | فصل منطق الوصول للبيانات  |
| **Service Pattern**     | منطق الأعمال              |
| **DTO Pattern**         | نقل البيانات بين الطبقات  |
| **Guard Pattern**       | حماية المسارات            |
| **Interceptor Pattern** | معالجة الطلبات/الاستجابات |
| **Event-Driven**        | التواصل غير المتزامن      |

### مثال على وحدة الطلبات

```typescript
// orders/orders.service.ts
@Injectable()
export class OrdersService {
  constructor(
    private readonly orderRepository: OrderRepository,
    private readonly restaurantService: RestaurantService,
    private readonly paymentService: PaymentService,
    private readonly notificationService: NotificationService,
    private readonly eventEmitter: EventEmitter2,
  ) {}

  async createOrder(dto: CreateOrderDto): Promise<Order> {
    // 1. التحقق من توفر المطعم
    // 2. حساب الإجمالي
    // 3. معالجة الدفع
    // 4. إنشاء الطلب
    // 5. إرسال الإشعارات
    // 6. إطلاق الحدث
  }
}
```

---

## 🌐 واجهة الويب (Web Frontend)

### التقنية: Angular 21 + TypeScript

### هيكل المجلدات

```
wfrontend/
├── src/
│   ├── app/
│   │   ├── components/            # ✅ المكونات المكتملة
│   │   │   ├── navbar/           # شريط تنقل المستخدمين
│   │   │   ├── footer/           # تذييل المستخدمين
│   │   │   ├── admin-navbar/     # شريط تنقل المسؤول
│   │   │   ├── admin-footer/     # تذييل المسؤول
│   │   │   ├── language-modal/   # نافذة اختيار اللغة
│   │   │   ├── theme-modal/      # نافذة اختيار الثيم
│   │   │   └── toast-container/  # حاوية الإشعارات
│   │   │
│   │   ├── services/              # ✅ الخدمات
│   │   │   ├── auth.ts           # المصادقة العامة
│   │   │   ├── user-auth.ts      # مصادقة المستخدم
│   │   │   ├── user-session.ts   # جلسة المستخدم
│   │   │   ├── admin-staff.ts    # إدارة الموظفين
│   │   │   ├── admin-language.ts # لغة لوحة التحكم
│   │   │   ├── admin-master-code.ts # كود التحكم
│   │   │   ├── language.ts       # إدارة الترجمات (7 لغات)
│   │   │   ├── theme.ts          # إدارة الثيمات (4 ثيمات)
│   │   │   ├── toast.ts          # الإشعارات
│   │   │   └── supportTickets.ts # تذاكر الدعم
│   │   │
│   │   ├── guards/                # ✅ حراس المسارات
│   │   │   └── auth.guard.ts     # Auth Guard + Login Guard
│   │   │
│   │   ├── layouts/               # ✅ التخطيطات
│   │   │   ├── user-layout/      # تخطيط المستخدمين
│   │   │   └── admin-layout/     # تخطيط المسؤول
│   │   │
│   │   ├── pages/                 # ✅ الصفحات
│   │   │   ├── admin/
│   │   │   │   ├── login/        # صفحة تسجيل الدخول
│   │   │   │   ├── dashboard/    # لوحة التحكم
│   │   │   │   ├── users/        # إدارة المستخدمين
│   │   │   │   └── system-settings/ # إعدادات النظام
│   │   │   └── user/
│   │   │       ├── profile/      # الملف الشخصي
│   │   │       └── legal/        # صفحات قانونية
│   │   │
│   │   ├── utils/                 # أدوات مساعدة
│   │   │
│   │   └── app.routes.ts          # المسارات
│   │
│   ├── assets/                    # الأصول
│   │   ├── images/
│   │   ├── icons/
│   │   └── i18n/                  # الترجمات
│   │       ├── ar.json
│   │       └── lb.json
│   │
│   └── styles.css                 # CSS Variables للثيمات
│
└── public/
```

### إدارة الحالة

```
┌─────────────────────────────────────────┐
│              Component                   │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│            Signal Store                  │
│         (State Management)              │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│              Service                     │
│          (API Communication)            │
└─────────────────────────────────────────┘
```

---

## 📱 تطبيقات الموبايل (Mobile Apps)

### التقنية: Flutter 3.10 + Dart

### الهيكل الموحد (Mono-repo)

```
mfrontend/
├── lib/
│   ├── main.dart                  # نقطة الدخول
│   ├── app.dart                   # تهيئة التطبيق
│   │
│   ├── core/                      # النواة المشتركة
│   │   ├── api/                   # عميل API
│   │   │   ├── api_client.dart
│   │   │   ├── interceptors/
│   │   │   └── endpoints.dart
│   │   │
│   │   ├── config/                # الإعدادات
│   │   │   ├── app_config.dart
│   │   │   ├── theme_config.dart
│   │   │   └── route_config.dart
│   │   │
│   │   ├── di/                    # حقن التبعيات
│   │   │   └── injection.dart
│   │   │
│   │   ├── utils/                 # الأدوات
│   │   │   ├── validators.dart
│   │   │   ├── formatters.dart
│   │   │   └── helpers.dart
│   │   │
│   │   └── constants/             # الثوابت
│   │
│   ├── shared/                    # المكونات المشتركة
│   │   ├── widgets/
│   │   │   ├── buttons/
│   │   │   ├── inputs/
│   │   │   ├── cards/
│   │   │   └── dialogs/
│   │   │
│   │   ├── models/                # النماذج المشتركة
│   │   │   ├── user.dart
│   │   │   ├── order.dart
│   │   │   └── restaurant.dart
│   │   │
│   │   └── services/              # الخدمات المشتركة
│   │       ├── auth_service.dart
│   │       ├── location_service.dart
│   │       └── notification_service.dart
│   │
│   ├── features/                  # الميزات
│   │   │
│   │   ├── auth/                  # المصادقة (مشترك)
│   │   │   ├── presentation/
│   │   │   │   ├── screens/
│   │   │   │   └── widgets/
│   │   │   ├── domain/
│   │   │   │   ├── entities/
│   │   │   │   └── usecases/
│   │   │   └── data/
│   │   │       ├── repositories/
│   │   │       └── datasources/
│   │   │
│   │   ├── customer/              # تطبيق العملاء
│   │   │   ├── home/
│   │   │   ├── restaurants/
│   │   │   ├── cart/
│   │   │   ├── orders/
│   │   │   ├── tracking/
│   │   │   └── profile/
│   │   │
│   │   ├── restaurant/            # تطبيق المطاعم
│   │   │   ├── dashboard/
│   │   │   ├── menu/
│   │   │   ├── orders/
│   │   │   └── analytics/
│   │   │
│   │   └── driver/                # تطبيق السائقين
│   │       ├── home/
│   │       ├── deliveries/
│   │       ├── navigation/
│   │       ├── earnings/
│   │       └── profile/
│   │
│   └── l10n/                      # الترجمة
│       ├── app_ar.arb
│       └── app_lb.arb
│
├── test/
│   ├── unit/
│   ├── widget/
│   └── integration/
│
└── assets/
    ├── images/
    ├── icons/
    └── fonts/
```

### نمط Clean Architecture

```
┌───────────────────────────────────────────────────────────┐
│                    Presentation Layer                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │   Screens   │  │   Widgets   │  │   BLoC/     │       │
│  │             │  │             │  │   Cubit     │       │
│  └─────────────┘  └─────────────┘  └─────────────┘       │
└───────────────────────────┬───────────────────────────────┘
                            │
                            ▼
┌───────────────────────────────────────────────────────────┐
│                      Domain Layer                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │  Entities   │  │  Use Cases  │  │ Repository  │       │
│  │             │  │             │  │  Interface  │       │
│  └─────────────┘  └─────────────┘  └─────────────┘       │
└───────────────────────────┬───────────────────────────────┘
                            │
                            ▼
┌───────────────────────────────────────────────────────────┐
│                       Data Layer                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │ Repository  │  │ Data Source │  │   Models    │       │
│  │   Impl      │  │             │  │             │       │
│  └─────────────┘  └─────────────┘  └─────────────┘       │
└───────────────────────────────────────────────────────────┘
```

---

## 🗄️ قاعدة البيانات

### PostgreSQL Schema

```
┌─────────────────────────────────────────────────────────────────┐
│                        DATABASE SCHEMA                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐    │
│  │    users     │     │  restaurants │     │   drivers    │    │
│  ├──────────────┤     ├──────────────┤     ├──────────────┤    │
│  │ id           │     │ id           │     │ id           │    │
│  │ email        │     │ name         │     │ user_id (FK) │    │
│  │ password     │     │ owner_id(FK) │     │ vehicle_type │    │
│  │ phone        │     │ address      │     │ license_no   │    │
│  │ role         │     │ location     │     │ status       │    │
│  │ avatar       │     │ rating       │     │ current_loc  │    │
│  │ is_verified  │     │ is_active    │     │ is_available │    │
│  │ created_at   │     │ created_at   │     │ rating       │    │
│  └──────┬───────┘     └──────┬───────┘     └──────┬───────┘    │
│         │                    │                    │             │
│         │    ┌───────────────┼────────────────────┘             │
│         │    │               │                                  │
│         ▼    ▼               ▼                                  │
│  ┌──────────────┐     ┌──────────────┐                         │
│  │    orders    │     │ menu_items   │                         │
│  ├──────────────┤     ├──────────────┤                         │
│  │ id           │     │ id           │                         │
│  │ user_id (FK) │     │ restaurant_id│                         │
│  │ restaurant_id│     │ name         │                         │
│  │ driver_id    │     │ description  │                         │
│  │ status       │     │ price        │                         │
│  │ total        │     │ image        │                         │
│  │ address      │     │ category     │                         │
│  │ created_at   │     │ is_available │                         │
│  └──────┬───────┘     └──────────────┘                         │
│         │                                                       │
│         ▼                                                       │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐    │
│  │ order_items  │     │   payments   │     │   reviews    │    │
│  ├──────────────┤     ├──────────────┤     ├──────────────┤    │
│  │ id           │     │ id           │     │ id           │    │
│  │ order_id(FK) │     │ order_id(FK) │     │ user_id (FK) │    │
│  │ item_id (FK) │     │ amount       │     │ target_id    │    │
│  │ quantity     │     │ method       │     │ target_type  │    │
│  │ price        │     │ status       │     │ rating       │    │
│  │ notes        │     │ transaction  │     │ comment      │    │
│  └──────────────┘     └──────────────┘     └──────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### Redis Cache Strategy

```
┌─────────────────────────────────────────────────┐
│              REDIS CACHE STRATEGY               │
├─────────────────────────────────────────────────┤
│                                                 │
│  Session Cache                                  │
│  └── user:session:{userId} → TTL: 24h          │
│                                                 │
│  Restaurant Cache                               │
│  └── restaurant:{id} → TTL: 1h                 │
│  └── restaurants:nearby:{lat}:{lng} → TTL: 5m  │
│                                                 │
│  Order Tracking                                 │
│  └── order:tracking:{orderId} → TTL: 2h        │
│                                                 │
│  Driver Location                                │
│  └── driver:location:{driverId} → TTL: 30s     │
│                                                 │
│  Rate Limiting                                  │
│  └── ratelimit:{ip} → TTL: 1m                  │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 🔐 الأمان والمصادقة

### طبقات الأمان

```
┌─────────────────────────────────────────────────────────────┐
│                     SECURITY LAYERS                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Layer 1: Transport Security                                 │
│  └── HTTPS/TLS 1.3                                          │
│  └── Certificate Pinning (Mobile)                           │
│                                                              │
│  Layer 2: API Security                                       │
│  └── Rate Limiting                                          │
│  └── Request Validation                                     │
│  └── CORS Configuration                                     │
│                                                              │
│  Layer 3: Authentication                                     │
│  └── JWT Access Tokens (15min)                              │
│  └── Refresh Tokens (7 days)                                │
│  └── OAuth 2.0 (Google, Apple, Facebook)                    │
│                                                              │
│  Layer 4: Authorization                                      │
│  └── Role-Based Access Control (RBAC)                       │
│  └── Permission Guards                                      │
│                                                              │
│  Layer 5: Data Security                                      │
│  └── Password Hashing (bcrypt)                              │
│  └── Data Encryption (AES-256)                              │
│  └── SQL Injection Prevention                               │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### تدفق المصادقة

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  Client  │────▶│  Login   │────▶│  Verify  │────▶│  Issue   │
│          │     │ Request  │     │ Creds    │     │  Tokens  │
└──────────┘     └──────────┘     └──────────┘     └────┬─────┘
                                                        │
     ┌──────────────────────────────────────────────────┘
     │
     ▼
┌──────────┐     ┌──────────┐     ┌──────────┐
│  Store   │────▶│  Attach  │────▶│  Access  │
│  Tokens  │     │  to Req  │     │ Resource │
└──────────┘     └──────────┘     └──────────┘
```

---

## 🔗 التكامل مع الخدمات الخارجية

### الخدمات المتكاملة

| الخدمة       | الغرض              | المزود                   |
| ------------ | ------------------ | ------------------------ |
| 🗺️ الخرائط   | الموقع والملاحة    | Google Maps API          |
| 💳 المدفوعات | معالجة الدفع       | Stripe, PayPal           |
| 📱 الإشعارات | Push Notifications | Firebase Cloud Messaging |
| 📧 البريد    | رسائل المعاملات    | SendGrid                 |
| 📞 SMS       | التحقق             | Twilio                   |
| 📁 التخزين   | الملفات والصور     | AWS S3                   |
| 🔍 البحث     | البحث المتقدم      | Algolia                  |
| 📊 التحليلات | تتبع الاستخدام     | Mixpanel                 |

---

## 📈 قابلية التوسع

### استراتيجية التوسع

```
                    ┌─────────────────────┐
                    │   Load Balancer     │
                    │     (NGINX)         │
                    └──────────┬──────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│   API Pod 1   │    │   API Pod 2   │    │   API Pod N   │
│   (NestJS)    │    │   (NestJS)    │    │   (NestJS)    │
└───────┬───────┘    └───────┬───────┘    └───────┬───────┘
        │                    │                    │
        └────────────────────┼────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│  PostgreSQL   │    │    Redis      │    │    S3         │
│  (Primary)    │    │   Cluster     │    │  Storage      │
│       │       │    └───────────────┘    └───────────────┘
│       ▼       │
│   Replica     │
└───────────────┘
```

### Kubernetes Deployment

```yaml
# مثال على تكوين Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: moien-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: moien-api
  template:
    spec:
      containers:
        - name: api
          image: moien/api:latest
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
```

---

## 📊 مقاييس الأداء المستهدفة

| المقياس                | الهدف           |
| ---------------------- | --------------- |
| ⏱️ زمن الاستجابة (P95) | < 200ms         |
| 🔄 الإتاحة             | 99.9%           |
| 📈 طلبات/ثانية         | > 10,000        |
| 💾 استخدام الذاكرة     | < 512MB per pod |
| 🔋 استهلاك CPU         | < 50% average   |

---

<div align="center">

**آخر تحديث: فبراير 2026**

[🔙 العودة للرئيسية](README.md) | [📚 التوثيق](docs/)

</div>
